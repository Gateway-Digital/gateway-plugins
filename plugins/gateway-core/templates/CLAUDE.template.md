# <PROJECT NAME>

<One-paragraph description: what this system does and who uses it.>

## Commands

- Install: `<command>`
- Run locally: `<command>`
- Test: `<command>` (single test: `<command>`)
- Lint/format: `<command>`

## Architecture

<Five lines maximum: the main components and how a request/data flows through them.
Link to docs/ for anything longer.>

## Conventions that differ from defaults

<Only the surprising ones - things a competent developer would get wrong without being
told. Everything else belongs in the stack plugin's conventions skill, not here.>

<!-- Keep this file under ~60 lines. Facts only; procedures live in skills.
     Bloated CLAUDE.md files measurably get ignored. -->
